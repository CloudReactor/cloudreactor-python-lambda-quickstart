cloudreactor-procwrapper==5.0.0

# Requirements for secrets resolution
# Note that boto3 is already included in the AWS Lambda python environment,
# so no need to add that here.
jsonpath-ng>=1.5.2
mergedeep>=1.3.4
python-dotenv>=0.17.1
pyyaml>=6.0
